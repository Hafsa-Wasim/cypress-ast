# Task 2 — Assertions, Aliases & Custom Commands

This task focuses on advanced Cypress testing concepts including assertions, aliases, custom commands, and reusable test setup using beforeEach hooks.

## Features Implemented

### Assertion Practice
The following assertions were implemented:
- `should('be.visible')`
- `should('have.text')`
- `should('have.attr')`

### Negative Assertion
A test was written using:
- `should('not.exist')`

### Alias Practice
Aliases were created using:
```javascript
.as('myAlias')